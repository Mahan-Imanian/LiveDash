{
  "name": "livedash-v7-chrome-extension",
  "version": "7.0.0",
  "private": true,
  "description": "LiveDash v7 Chrome extension new tab command center.",
  "scripts": {
    "check": "node --check scripts/default-state.js && node --check scripts/storage.js && node --check scripts/app.js && node --check scripts/popup.js && node --check scripts/options.js && node --check background.js && node scripts/validate-extension.js",
    "build": "npm run check",
    "package": "npm run build && node scripts/package-extension.js"
  },
  "keywords": [
    "chrome-extension",
    "new-tab",
    "dashboard",
    "bookmarks",
    "productivity"
  ],
  "license": "MIT"
}
