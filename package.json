{
  "name": "livedash-v12-widgetify-style-extension",
  "version": "12.1.0",
  "private": true,
  "description": "LiveDash personalized new tab dashboard Chrome extension.",
  "scripts": {
    "check": "node --check scripts/default-state.js && node --check scripts/storage.js && node --check scripts/app.js && node --check scripts/popup.js && node --check scripts/sidepanel.js && node --check scripts/options.js && node --check background.js && node scripts/validate-extension.js",
    "build": "npm run check",
    "package": "npm run build && node scripts/package-extension.js"
  },
  "keywords": [
    "chrome-extension",
    "new-tab",
    "dashboard",
    "widgets",
    "productivity"
  ],
  "license": "MIT"
}
